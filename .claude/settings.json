{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/siddharth/para/personal-projects/mac_setup && git diff --quiet && git diff --cached --quiet || (git add -A && git commit -m 'chore: sync mac setup configs' && git push)",
            "timeout": 30,
            "statusMessage": "Syncing mac setup to remote..."
          }
        ]
      }
    ]
  }
}
